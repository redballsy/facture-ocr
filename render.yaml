services:
  - type: web
    name: facture-ocr
    runtime: python
    buildCommand: apt-get install -y tesseract-ocr tesseract-ocr-fra && pip install -r requirements.txt
    startCommand: python app.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0